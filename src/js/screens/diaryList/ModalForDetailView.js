
import React, { useEffect } from 'react'
import { Modal, Button } from 'react-bootstrap'

const ModalForDetailView = ({diaryView})=>{
    // return(
    //     <Modal id="detailView" show={diaryView} onHide={() => setTop10Modal(false)} size="lg" animation={browser === 'IE' ? false : true} aria-labelledby="contained-modal-title-vcenter" centered>

    //     </Modal>
    // )
}
export default ModalForDetailView